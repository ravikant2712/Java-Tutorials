package java25;

import static java.lang.IO.println;

// Primitive types in patterns, instanceof and switch are not supported at language level '25
public class PrimitivePatternMatching {

    static void main() {

        Object obj = 5;
/*        switch (obj) {
            case int i -> println("It's an int: " + i);
            case double d -> println("It's a double: " + d);
            default -> println("Other type");
        }*/
    }
}
